# ~/.omp/agent/.env
#
# omp eagerly loads this file into the process environment at startup. The
# alibaba-maas CLI also reads it as a fallback when $ALIBABA_TOKEN_PLAN_API_KEY
# is not exported in your shell.

ALIBABA_TOKEN_PLAN_API_KEY=sk-sp-REPLACE-WITH-YOUR-KEY
